#!/usr/bin/env bash
# Self-test entry for the Phase 18 A/B gate (delegates to the gate's assert tests).
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec perl "$here/mtpg_ab_gate" --selftest
