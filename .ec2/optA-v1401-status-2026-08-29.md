# Option A on libxtc v1.40.1: single-fiber strand FIXED; concurrent-commit starvation REMAINS

Date 2026-08-29 (mala c6id.8xlarge NVMe, libxtc v1.40.1 rev ebc430c, PG debugoptimized).

## v1.40.1 fixed the reported bug (validated)
The libxtc v1.40.1 idle-poll-dispatch fix (exec.c: the executor's idle poll now
dispatches the io events it reaps instead of dropping them) resolves the single-fiber
fsync-completion strand I reported:
 - FORCED-OFFLOAD (XTC_AIO_FORCE_OFFLOAD=1) scale-100 init: was 100% wedge on v1.40.0 ->
   COMPLETES on v1.40.1.
 - NATIVE io_uring scale-100 init: was 1/8 wedge on v1.40.0 -> 0/8 on v1.40.1.
Confirmed the fix is compiled in (exec.c:267 __xtc_loop_dispatch_event in the idle-poll
branch; comment names the LWLock-across-fsync stranding case).

## A DIFFERENT wedge remains: concurrent-commit starvation at 64 write clients
fibers0 (pooled_protocol_carriers=0) 64-client TPC-B write load:
 - pgbench runs at 1029 tps for the first ~2s, then collapses to 0.0 tps FOREVER.
 - Server-side commits freeze (delta ~2 commits / 4s).
 - `select 1` from a fresh conn mostly RECOVERS (single-probe blips) -- so it is not a
   total hard wedge of the whole server; it is the COMMIT PATH that starves.
 - gdb: the active committer fibers are parked in
     exec_simple_query("END;") -> CommitTransaction -> XLogFlush ->
     LWLockAcquireOrWait(WALWriteLock, EXCLUSIVE) -> ProcSemaphoreWaitFiber ->
     xtc_pg_wait_fd(sem_wake_fd)
   waiting for WALWriteLock to become free (the group-commit "wait for someone else to
   flush past my LSN" path).  NO fiber holds WALWriteLock; 29-31 carrier loops sit idle in
   xtc_io_poll.  So the last holder RELEASED and the LWLock-release wake
   (LWLockWakeup -> ProcWakeSemaphore -> sem_fiber eventfd + xtc_proc_wake) to these
   parked LWLockAcquireOrWait fiber-waiters was LOST -> they never resume -> commits
   stall -> thundering-herd collapse after the first burst.

This is a DIFFERENT wake site than v1.40.1 fixed: it is the LWLock-release -> fiber-waiter
wake (ProcWakeSemaphore path in proc.c), not the aio/blocking completion resume.  It may
be: (a) another libxtc idle-poll/steal edge the v1.40.1 fix does not cover for the
sem_wake_fd readiness path; (b) a race in PG's ProcWakeSemaphore sem_fiber
arm/pending/xtc_proc_wake hand-off under many concurrent LWLockAcquireOrWait waiters; or
(c) a group-commit interaction (many waiters on WALWriteLock, one flusher, wake fan-out
losing a waiter).  Needs its own root-cause: instrument which of the parked
LWLockAcquireOrWait waiters got a ProcWakeSemaphore and whether xtc_proc_wake reached the
owning loop; capture the WALWriteLock state (held vs free) at the freeze.

## Status of option A
- Model correct; single-fiber fsync coupling fixed; write-fsync no longer inline-blocks.
- BLOCKED on the concurrent-commit starvation before the default can flip to the fiber
  model.  Do NOT flip the -1 auto default yet -- 64-client write load collapses to 0 tps.
- fork baseline (same box/config): ~45,576 tps @ 64 clients -- the number to beat.

## Next
1. Root-cause the LWLock-release -> fiber-waiter lost wake under concurrent commit
   (instrument ProcWakeSemaphore + xtc_proc_wake reach; lock-held-vs-free at freeze).
2. If libxtc: file with the LWLockAcquireOrWait/sem_wake_fd repro (64-client TPC-B, first
   burst then 0 tps).  If PG: fix the sem_fiber wake hand-off / add the xtc_loop_wake nudge
   on the LWLock-release fiber-wake.
3. Re-validate 64/192/256 clients + sustained load, THEN flip the default + full matrix.

## UPDATE: the concurrent-commit wedge is a STRANDED WALWriteLock HOLDER (not a lost release-wake)
gdb at the 64-client freeze: WALWriteLock state.value=0x80042000 -> HELD EXCLUSIVE.  The
holder is a client backend fiber on NO OS thread (BackendMain=0, exec_simple=0, XLogWrite=0,
xtc_aio=0 in the whole bt) -- all 64 client fibers are libxtc-parked; one holds WALWriteLock
and is never resumed; 31 loops idle in xtc_io_poll; the WAL writer fiber blocks on the lock.
Same END STATE as the v1.40.0 bug (stranded holder, loops idle) but survives v1.40.1 and is
CONCURRENCY-gated (single-fiber init 0/8; 64-fiber commit collapses after ~2s every time).
Filed as a v1.40.1 follow-up: plan_docs/phase16_audits/LIBXTC_FIBER_RESUME_CONCURRENT_COMMIT.md
+ /tmp/libxtc-fiber-resume-concurrent-commit-2026-08-29.md.  Likely a second reap-and-drop
poll site or a steal/rebalance race under high migratable-fiber churn (the v1.40.1 root
cause was a stolen-to-peer fiber; at 64 fibers with eager rebalance the steal churn is high).

## UPDATE 2026-08-30: v1.40.2 fixes the OFFLOAD race but the NATIVE-path collapse REMAINS
libxtc v1.40.2 (rev ed60a396, fix commit dec8428) added `_Atomic done` to blk_work so
xtc_blocking_run re-parks on a spurious wake instead of tearing down the completion pipe
(a close-vs-write race + stack UAF the libxtc team caught with TSan -- confirmed my
hypothesis 2).  VALIDATED the fix is compiled in (blocking.c:49,297).  BUT:
 - A) forced-offload init: still completes (v1.40.1 behavior intact).
 - B) 64-client TPC-B sustained: STILL collapses -- ~2 nonzero progress samples then 0.0
   tps for the rest (19 samples).  192 and 256 same, no PANIC.
 - gdb at the collapse: iou-wrk=25 / blk_worker=0 / xtc_blocking=0  -> we are on the
   NATIVE io_uring FSYNC path, NOT the offload path v1.40.2 fixed.  32 loops idle in
   xtc_io_poll, BackendMain=0 (all client fibers parked), commits frozen.  The stranded
   fiber is native-path.
The libxtc reply explicitly noted the native io_uring FSYNC path "was not subject to this
specific [offload] race."  So there is a SEPARATE native-path resume gap under concurrent
commit that v1.40.2 does not close.  Filed the v1.40.2 follow-up; the team asked for
exactly (a) native-vs-offload [answer: NATIVE] and (b) a TSan run -- pursue the TSan build
next (they said TSan found the offload one in minutes).
Option A default flip stays BLOCKED on this native-path concurrent-commit resume fix.
